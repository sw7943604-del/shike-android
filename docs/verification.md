# 验证记录

## 环境

- Windows，Temurin JDK17.0.20.1，Gradle8.13，AGP8.11.1。
- Android SDK36；Google APIs x86_64 API36模拟器，1080×2400。
- 目标手机一加13 / Android16尚未连接，因此模拟器结果不代表已完成一加实机验证。

## 已执行检查

- 真实XLS：21项上课安排、10项作息；所有教师为空；起始日2026-09-06；单周11/13/15/17正确。
- 原子导入：无效作息不改变旧课表，成功后课程/学期/作息一次提交；预览可恢复。
- 页面流程：预览不覆盖，确认后保存，Activity重建后课程保留，主题选择保留。
- Android XML差异：已复现并修复DOCX解析器不支持桌面XML特性的问题；保留DTD/外部实体拒绝。
- PDF文字/扫描：合成中文表格样本分别识别两项课程；OCR漏周单位时明确警告待核对。
- 小组件：测试宿主实际绑定TodayWidget/NextWidget并收到本地课表状态，测试权限结束后撤回。
- 网页解析：嵌套表格、选中学期、学校周日起始校历有核心回归测试。
- 视觉检查：已查看真实XLS导入后的周课表截图，课程列、行、导航与系统栏正常。

## 修复记录

1. Windows中文路径造成测试类找不到，构建脚本使用可重复的英文目录联接。
2. 不完整JBR缺jlink，构建切换到完整JDK17。
3. 星期天误映射为周一，已修复并回归。
4. 嵌套HTML表格漏读，改为逐表解析并去重。
5. 长横线作息导致数组越界，统一格式、校验时刻并添加设备回归。
6. Android16返回手势和系统栏适配，Portal使用OnBackPressedDispatcher及Insets。
7. OCR无单位数值被当表头丢弃，保留课程块内范围并明确标为待核对。
8. Word无表格段落被拼成一行，保留段落换行。

## 验证限制

- 未在真实学校手机登录会话中验证统一认证、验证码、校内网络限制及门户弹窗流程；未复制电脑Cookie。
- PDF样本为受控合成样本，不能据此保证任意文档、倾斜扫描、跨页合并表格准确。
- 组件不使用精确闹钟，系统周期刷新存在延迟。
- 默认是个人安装的debug签名APK，未发布至任何服务。

## 最终结果（2026-09-06）

- 核心测试：19项通过，0失败。
- Android16仪器测试：9项通过，0失败。包括实际WebView执行页面读取并形成持久预览；HTML内容为模拟学校结构的测试样本，不是真实统一认证会话。
- `assembleDebug`与`lintDebug`通过。静态检查仍有库版本、外部库、国际化和自定义绘图等非阻塞警告，未禁用检查或创建基线。
- APK：`outputs/shike-0.1.0.apk`，59,883,359字节，Android最低26、目标36，含arm64-v8a等架构。
- `apksigner verify --verbose`通过，APK Signature Scheme v2有效。
- SHA256：`6B582E40972F425491BB251820DF50DA70336FDFE57F876DB67E11CFD481AF9F`。
- 检查正式APK内不含private-schedule测试文件；个人样本与outputs已被Git忽略。

## 0.1.1 教务入口修复（2026-09-06）

- 修复仅允许 `/jsxsd/` 导致本科教务 HTTP 站点首页和其他认证路径被拦截的问题；仍精确限制 HTTP 主机为 `csujwc.its.csu.edu.cn`。
- 新旧 WebView 导航回调统一判断；拦截提示显示协议及主机，不包含登录票据参数。
- 回归测试先在旧版复现教务首页被拦截；修复后 Android 16 全部 10 项仪器测试通过，覆盖允许入口、拒绝外部域名及敏感参数不显示。核心 19 项测试通过，lint 0 errors / 19 warnings。
- 尚未在用户手机上验证实际门户点击及统一认证完整链路；本次确认的是地址检查缺陷。
- versionCode 2 / versionName 0.1.1；新旧 APK 签名证书相同，可覆盖升级。
- APK：`outputs/shike-0.1.1.apk`，SHA256：`A5DC73FFE9388DDDB7638C2BA1A23DD7BD138BDA51896FB3D1BCC865520B5DAE`。

## 0.1.2 网页空格兼容（2026-09-06）

- 用户确认 0.1.1 已能进入教务，直接进入学期课表仍无识别结果；可见节次是 1 - 2 等数字范围。
- 复现 HTML 不换行空格导致表头或节次正则不匹配、整表零课程；规范 Unicode 空格，同时保留课程换行。测试样本含同格两门课程、教师和分段周次。
- 用户网页源码尚未取得，不能仅凭截图认定真实页面一定含不换行空格。读取失败现在报告表格、星期/节次表头和不可访问框架数量，辅助后续定位。
- 核心 20 项测试通过；Android 16 全部 10 项测试通过，WebView 测试包含 3000px 宽度和 HTML 空格，验证超出屏幕仍可读取、预览持久保存。assembleDebug / lintDebug 通过。
- APK 版本 0.1.2 / versionCode 3；签名验证通过且与旧版一致，可覆盖安装。
- `outputs/shike-0.1.2.apk` SHA256：`18417020F655865717CB82BDF58164A9607E45518023886280866D298CDB399B`。
